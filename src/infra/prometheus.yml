global:
  scrape_interval:     15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'eos-queue'

scrape_configs:
  - job_name: 'eos-queue'
    static_configs:
      - targets: ['host.docker.internal:3000']
    metrics_path: '/api/metrics/prometheus'
    scrape_interval: 10s